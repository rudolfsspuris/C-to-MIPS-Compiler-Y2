#ifndef forward_declaration_hpp
#define forward_declaration_hpp


//FORWARD DECL
#include <vector>
#include <string>
std::vector<std::string> global_variables;
int indent_count(0);
bool in_iteration(false);
bool newline_selective(false);
bool in_function(false);
int primary_parantheses(0);
bool function(false);


#endif
